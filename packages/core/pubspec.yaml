name: core
description: Core module for shared logic
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.7.0
  dio: ^5.7.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  battery_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

flutter:
  generate: true
