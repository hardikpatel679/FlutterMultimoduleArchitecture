name: flutter_basics
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  dio: ^5.7.0
  get_it: ^7.7.0
  pretty_dio_logger: ^1.3.1
  
  # Local Modules
  core:
    path: packages/core
  domain:
    path: packages/domain
  network:
    path: packages/network
  login_module:
    path: packages/features/login_module
  flutter_localizations:
    sdk: flutter
  graphql_flutter: ^5.3.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.0
  mocktail: ^1.0.4
  build_runner: ^2.4.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/mocks/

